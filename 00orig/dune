(cram
 (deps mini.exe fac.exe fack.exe))

(rule
 (deps mini.ml)
 (targets mini.s mini.o mini.exe.startup.s mini.exe)
 (mode
  (promote (until-clean)))
 (action
  (run
   ocamlopt
   -dstartup
   -S
   %{deps}
   -nopervasives
   -verbose
   -o
   mini.exe
   -cclib
   -lasmrun
   -cclib
   -lm)))

(rule
 (deps fac.ml)
 (targets fac.s fac.o fac.exe.startup.s fac.exe)
 (mode
  (promote (until-clean)))
 (action
  (run
   ocamlopt
   -dstartup
   -S
   %{deps}
   -nopervasives
   -verbose
   -o
   fac.exe
   -cclib
   -lasmrun
   -cclib
   -lm)))

(rule
 (deps fack.ml)
 (targets fack.s fack.o fack.exe.startup.s fack.exe)
 (mode
  (promote (until-clean)))
 (action
  (run
   ocamlopt
   -dstartup
   -S
   %{deps}
   -nopervasives
   -verbose
   -o
   fack.exe
   -cclib
   -lasmrun
   -cclib
   -lm)))

(cram
 (deps fac_acc.exe))

(rule
 (deps fac_acc.ml)
 (targets fac_acc.s fac_acc.o fac_acc.exe.startup.s fac_acc.exe)
 (mode
  (promote (until-clean)))
 (action
  (run
   ocamlopt
   -dstartup
   -S
   %{deps}
   -nopervasives
   -verbose
   -o
   fac_acc.exe
   -cclib
   -lasmrun
   -cclib
   -lm)))
